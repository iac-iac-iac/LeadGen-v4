
# 📋 CONTEXT FILE — Lead Generation Automation System (AI‑ready, PyQt6)

> Этот файл — инструкция для разработчиков и ИИ‑агентов, работающих над проектом.  
> Здесь описаны цели, архитектура, требования к качеству, UX и дальнейшему развитию системы.[file:14][web:15]

---

## 1. Краткое резюме проекта

Проект: десктоп‑система для автоматизации лидогенерации в холодных продажах.[file:14]  
Источник данных: выгрузки компаний из Яндекс.Карт/2GIS через сервис Webbee AI (TSV/CSV).  
Задача: из «сырых» файлов с десятками колонок и шумом сделать очищенный, объединённый CSV, готовый к импорту в Битрикс24, с корректными телефонами, удалёнными дубликатами и нужной разметкой.[file:14]

Интерфейс: современное кроссплатформенное приложение на **PyQt6**, ориентированное на роль «Аналитик лидогенерации».[file:14]  
Продукт должен быть **готов к работе из коробки**, расширяемым и поддерживаемым во времени.

---

## 2. Роль ИИ и ожидания от него

### 2.1. Что должен уметь делать ИИ

1. Проектировать и уточнять архитектуру  
   - Предлагать структуру модулей, слоёв, точек расширения и зависимостей.[file:14]  
   - Соблюдать разделение на слои: бизнес‑логика (обработка данных), GUI (PyQt6), БД, утилиты.[file:14]

2. Писать рабочий код «под ключ»  
   - Реализовывать модули по этому context‑файлу: чтение/запись CSV, валидация, маппинг, GUI‑экраны.[file:14]  
   - Учитывать edge‑кейсы: формат телефонов, дубликаты, UTM‑метки, пустые значения и т.д.[file:14]

3. Обеспечивать современный дизайн на PyQt6  
   - Использовать компоновки (QVBoxLayout/QHBoxLayout/QGridLayout), QTableView, QProgressBar, QMessageBox, QFileDialog.[file:14]  
   - Делать интерфейс «живым»: понятные подписи, состояние кнопок, индикаторы, обработка ошибок.

4. Оптимизировать и стабилизировать продукт  
   - Предлагать решения, устойчивые к большим объёмам данных (десятки тысяч строк).[file:14]  
   - Добавлять разумное логирование, обработку исключений, валидацию входных данных.[file:14]

5. Поддерживать и развивать код  
   - В новых задачах исправлять баги и несостыковки без поломки общей архитектуры.  
   - Добавлять новые модули (аналитика, Webbee/Битрикс API, ЛПР и др.) как расширения, а не «переписывание всего».[file:14]

### 2.2. Жёсткие требования к ИИ‑генерации

- Не ломать уже описанную архитектуру без явного запроса.  
- Сохранять читаемость кода: понятные имена, базовые docstring, отсутствие «магии» и захардкоженных значений там, где можно использовать конфиги.[file:14]  
- Не вводить новые внешние зависимости без пояснения и принятия решения.  
- Учитывать, что результат будет запускаться на рабочих машинах аналитиков (Windows в приоритете, но желательно кроссплатформенность).[file:14]

---

## 3. Бизнес‑контекст и роль пользователя

Роль: **Аналитик лидогенерации**.[file:14]

Что делает пользователь сейчас (as‑is):

1. Ищет в Яндекс.Картах/2GIS компании по запросам вида `[Сегмент] + [Регион]` (например, «Мебель на заказ Москва ЗАО»).[file:14]  
2. Передаёт ссылки в Webbee AI и скачивает TSV/CSV‑выгрузки.[file:14]  
3. Вручную чистит файлы: удаляет лишние колонки, приводит телефоны в нормальный вид, удаляет дубли и мусор.[file:14]  
4. Собирает данные в один CSV и загружает в Битрикс24 как лиды.[file:14]

Система должна заменить шаги 3–4 и сделать процесс более быстрым, предсказуемым и защищённым от ошибок.[file:14]

---

## 4. Цели системы (to‑be)

### 4.1. Основные цели MVP

1. Принять на вход несколько TSV/CSV‑файлов от Webbee AI.[file:14]  
2. Очистить и нормализовать телефонные номера по строгим правилам.[file:14]  
3. Удалить лишние колонки и строки без нужной информации.[file:14]  
4. Объединить данные в один набор с добавлением поля «Источник телефона» (имя файла).[file:14]  
5. Преобразовать данные к структуре CSV для импорта в Битрикс24 (64 колонки, часть пустая).[file:14]  
6. Распределить лиды по менеджерам по круговому алгоритму (round‑robin).[file:14]  
7. Предоставить пользователю удобный PyQt6‑интерфейс для этого процесса.[file:14]

### 4.2. Цели следующих версий

- Аналитика по результатам обзвона и сделок на основе экспортов из Битрикс (LEAD/DEAL).[file:14]  
- Генерация и управление ссылками для Яндекс.Карт.  
- Интеграция с Webbee AI API (автоматический запуск парсинга и загрузка).  
- Интеграция с Битрикс24 API (автоимпорт лидов).  
- Автоматизация поиска ЛПР (через API и AI‑парсинг сайтов).[file:14]

---

## 5. Технический стек и общие требования

### 5.1. Языки и платформы

- Backend: **Python 3.11.9**.[file:14]  
- GUI: **PyQt6** (чистый PyQt6, без смешивания с Tkinter/customtkinter).[file:14]  
- БД: встроенный **SQLite** через модуль `sqlite3`.[file:14]  
- Целевая платформа: Windows (приоритет), желательно работоспособность на macOS и Linux.[file:14]

### 5.2. Основные библиотеки

- `pandas` — чтение/запись CSV/TSV, работа с DataFrame.[file:14]  
- `openpyxl` — экспорт отчётов в Excel (этап аналитики).[file:14]  
- `sqlite3` — хранение статистики и списка менеджеров.[file:14]

Дополнительно (опционально, для будущих этапов):  

- Библиотека для генерации PDF‑отчётов.  
- Библиотеки для построения диаграмм (если не использовать только Excel).[file:14]

---

## 6. Архитектура и структура проекта

### 6.1. Общая структура каталогов

```
lead_generation_system/
│
├── main.py                  # Точка входа, запуск PyQt6 GUI
├── config.json              # Настройки (менеджеры, пути, дефолты)
├── requirements.txt         # Python‑зависимости
├── README.md                # Инструкция по установке и запуску
├── project_context.md       # Этот файл (контекст для людей и ИИ)
│
├── modules/                 # Бизнес‑логика
│   ├── **init**.py
│   ├── data_processor.py    # Очистка и объединение CSV/TSV
│   ├── phone_validator.py   # Валидация и нормализация телефонов
│   ├── bitrix_mapper.py     # Маппинг колонок для Битрикс24
│   ├── analytics.py         # Метрики и аналитика (след. этап)
│   ├── chart_generator.py   # Диаграммы (след. этап)
│   └── report_exporter.py   # Отчёты в Excel/PDF (след. этап)
│
├── gui/                     # PyQt6‑интерфейс
│   ├── **init**.py
│   ├── main_window.py       # Главное окно
│   ├── file_loader.py       # Логика выбора и отображения файлов
│   ├── preview_table.py     # Таблица предпросмотра
│   ├── progress_bar.py      # Виджет прогресса (обёртка над QProgressBar)
│   └── styles.py            # Стили и тема интерфейса
│
├── database/
│   ├── **init**.py
│   ├── db_manager.py        # Подключение и операции с SQLite
│   └── models.py            # Описание таблиц и вспомогательные функции
│
├── utils/
│   ├── **init**.py
│   ├── url_cleaner.py       # Очистка UTM‑меток и параметров URL
│   ├── logger.py            # Логирование операций
│   └── config_loader.py     # Загрузка и сохранение config.json
│
├── data/
│   ├── input/               # Входные файлы от Webbee AI
│   ├── output/              # Выходные CSV для Битрикс24
│   ├── reports/             # Отчёты (Excel/PDF)
│   └── database.db          # Файл SQLite
│
└── tests/
    ├── test_phone_validator.py
    ├── test_data_processor.py
    └── test_bitrix_mapper.py
```

Архитектурный принцип: бизнес‑логика не должна зависеть от конкретного GUI. PyQt6 вызывает функции из `modules/`, но не наоборот.[file:14]

---

## 7. Данные и правила обработки

### 7.1. Входные файлы (Webbee AI)

- Формат: TSV (Tab‑Separated Values), кодировка UTF‑8.[file:14]
- 124+ колонок. Ключевые:
  - `Название` (название компании),
  - `Адрес`,
  - `phone_1`, `phone_2`,
  - `Category 0-4`,
  - `companyUrl`,
  - `vkontakte`, `telegram`, `whatsapp`,
  - координаты, рейтинг, время работы, транспорт и множество дополнительных `phone_*`.[file:14]

Проблемы:

- телефоны в научной нотации (`7.8005001695e+10`),
- мусор — пробелы, скобки, +, буквы,
- дубликаты номеров между строками,
- много нерелевантных колонок.[file:14]

### 7.2. Основные правила обработки телефонов

- Целевой формат: **строка из 11 цифр, начинается с 7 или 8**, без знака `+` и любых других символов.[file:14]
- Используются только `phone_1` и `phone_2`; остальные `phone_*` игнорируются.[file:14]
- На входе возможна научная нотация — нужно приводить к строке (либо через `dtype=str` при чтении, либо через конвертацию).[file:14]
- Битые номера (короче 10 цифр, не начинаются с 7/8, содержат буквы после очистки) — причина удалить строку целиком.[file:14]

### 7.3. Дубликаты и строки без телефонов

- Дубликат определяется по совпадению номера телефона (phone_1 или phone_2) между строками.[file:14]
- При дубликатах оставляется первая встреченная строка, остальные удаляются.[file:14]
- Компании, у которых и `phone_1`, и `phone_2` пустые (после очистки), удаляются.[file:14]

### 7.4. «Источник телефона»

- Обязательная колонка `Источник телефона`.[file:14]
- Содержит имя исходного файла, например: `оборудование по обработке металла_МСК_04.02.26.csv`.[file:14]
- Используется для:
  - идентификации собственных лидов в Битрикс,
  - фильтрации при аналитике,
  - группировки по сегментам/регионам.[file:14]

---

## 8. Выходные данные для Битрикс24

### 8.1. Общий формат

- CSV, UTF‑8, разделитель — запятая.[file:14]
- Ровно 64 колонки по структуре Битрикс24, даже если большинство пустые.[file:14]
- Неиспользуемые колонки заполняются пустой строкой (`""`), а не `NaN`/`None`/`NULL`.[file:14]

### 8.2. Маппинг полей (основные)

- `Название лида` = `[Category 0] - [Название]`.[file:14]
- `Рабочий телефон` = очищенный `phone_1`.[file:14]
- `Мобильный телефон` = очищенный `phone_2` (если есть).[file:14]
- `Адрес` = как в исходных данных.[file:14]
- `Корпоративный сайт` = `companyUrl` без UTM‑меток и лишних параметров.[file:14]
- `Контакт Telegram` = username без `https://t.me/` и без `@` (можно при импорте добавить `@`).[file:14]
- `Контакт ВКонтакте` = username без `https://vk.com/`.[file:14]
- `Название компании` = `Название`.[file:14]
- `Комментарий` = пусто в MVP (позже — ЛПР).[file:14]
- `Стадия` = «Новая заявка» (статичное значение).[file:14]
- `Источник` = «Холодный звонок» (статичное значение).[file:14]
- `Тип услуги` = «ГЦК» (статичное значение).[file:14]
- `Источник телефона` = имя исходного файла.[file:14]

---

## 9. GUI (PyQt6) — требования к UX

### 9.1. Главное окно

Блоки:

1. **Загрузка файлов**
    - Кнопка «Выбрать файлы» + поддержка выбора нескольких TSV/CSV.[file:14]
    - Список выбранных файлов с возможностью удалить любой из них.
    - Счётчик: «Загружено: N файлов».[file:14]
2. **Настройки менеджеров**
    - Многострочное текстовое поле: один менеджер на строку.[file:14]
    - Кнопка «Сохранить список».
    - Валидация: минимум один менеджер, недопустимо пустое поле.[file:14]
3. **Кнопка «Очистить и объединить» + прогресс**
    - Размер и стиль делают её визуальным центром действия.[file:14]
    - Активируется только при наличии файлов и сохранённого списка менеджеров.[file:14]
    - При работе: прогресс‑бар + текст «Обработка файла X из Y…».[file:14]
4. **Предпросмотр результатов**
    - Таблица (QTableView) с первыми 10 строками очищенного набора:
        - Колонки: Название лида, Рабочий телефон, Адрес, Ответственный, Источник телефона.[file:14]
    - Под таблицей: строка статуса («Обработано: … | Уникальных номеров: … | Дубликатов удалено: …»).[file:14]
5. **Экспорт**
    - Кнопка «Экспортировать для Битрикс».
    - Диалог сохранения файла и генерация итогового CSV.[file:14]

### 9.2. Ошибки и уведомления

- Использовать QMessageBox (информационные, предупреждения, критические).
- Понятные тексты: указывать, в каком файле/шаге ошибка и что можно сделать.[file:14]
- Не допускать «тихих» провалов — ошибка либо показана пользователю, либо записана в лог (лучше оба).[file:14]

---

## 10. База данных и статистика

SQLite база (`data/database.db`) хранит:

- Таблица `statistics` — информация о загрузках (имя файла, дата, количество лидов, уникальных номеров, сегмент, регион и т.п.).[file:14]
- Таблица `managers` — список менеджеров и их статус (активен/неактивен).[file:14]
- Таблица `processing_history` — история обработок файлов (количество строк, дубликатов, время обработки).[file:14]

Эти данные используются для аналитики и отладки.[file:14]

---

## 11. Планы развития (summary)

1. **Этап 1 (MVP):** ядро обработки и PyQt6‑GUI, готовый CSV для Битрикс.[file:14]
2. **Этап 2:** GUI‑улучшения, оптимизация UX, устранение багов.
3. **Этап 3:** аналитика по экспортам Битрикс, отчёты и диаграммы.[file:14]
4. **Этап 4+:** генератор ссылок, интеграции Webbee/Битрикс API, поиск ЛПР и накопительная аналитика.[file:14]

---

## 12. Как ставить задачи ИИ‑модели по этому файлу

Примеры:

- «Реализуй модуль `modules/phone_validator.py` по разделу 7.2 этого файла.»
- «Сгенерируй PyQt6‑экран главного окна по разделу 9 (GUI), не меняя архитектуру.»
- «Найди узкие места в производительности `data_processor` (обработка больших файлов) и предложи оптимизацию.»
- «Добавь модуль аналитики из Этапа 3, сохранив структуру проекта и формат входных/выходных файлов.»

Всегда прикладывать актуальный `project_context.md` к запросу и указывать, что менять можно, а что — нет.

---
